# 臺灣史記憶關係圖：右鍵編輯功能規劃

> 目標：在關係圖上，**按右鍵**即可「改顏色／新增／編輯／刪除」，
> 變更可保留、可還原、可匯出合併回正式檔。純前端實作，不需安裝任何東西。
>
> 狀態：
> - **第一階段已實作（2026-09-04，`臺灣史記憶關係圖_v7.html` v10）**：
>   範圍＝**衛星掛點（節點往下展開的那一串小點）**。見下方 §0。
> - 其餘（節點本身的新增／刪除／改色）仍為規劃中。

---

## 0. 已實作：衛星掛點右鍵編輯（v7 / v10）

衛星＝★重點字模式下，每個節點往下垂的那一串小圓點。它原本 100% 由說明文自動算出來，
現在可以手改，且**原始資料區 `N`／`E` 完全沒動**——改的是疊在上面的一層補丁。

### 操作

| 右鍵位置 | 選單 |
|---|---|
| **一顆衛星上** | 🎨 紅／藍／灰三色 ・ ✎ 改文字 ・ ↑↓ 移動順序 ・ ＋ 在下方插入 ・ 🗑 刪除這顆 ・ ▤ 編輯整串 ・ ↩ 還原自動 |
| **節點上** | ▤ 編輯衛星清單（表單） ・ ＋ 新增一顆 ・ 🚫 這個節點不掛衛星 ・ ↩ 還原自動計算 |
| **空白處** | 💾 匯出變更 JSON ・ 📥 匯入變更檔 ・ ↩ 全部還原成自動 |

- 清單編輯器：每列＝顏色下拉＋文字框＋↑↓✕，最下方「＋ 新增一顆」；
  標題下方永遠列出**自動值**，改壞了照著改回來或直接按「還原自動」。
- 文字含「－」即成配對點（例：`臺北－瑠公圳`），與自動算出來的配對點顯示方式一致。
- 手改過的節點，鏈頭會出現一個小 `✎`。
- 衛星只在 ★重點字 模式看得見，所以編輯器存檔後會自動把該模式打開。

### 顏色語意

紅 `hl`＝核心重點｜藍 `hb`＝人名・年代・地區｜灰 `loc`＝地點・補充。

### 補丁格式（匯出的 JSON）

```json
{ "ver": 1, "base": "v7",
  "kw": { "e3Ba": [ {"d":"規模最大","k":"hl"}, {"d":"濁水溪","k":"hl"} ] } }
```

一個節點的陣列＝**整串取代**該節點的自動衛星；空陣列＝這個節點不掛衛星；
沒出現在 `kw` 裡的節點＝維持自動計算。

- 存放：`localStorage["twhist_kw_v7"]`，同一台電腦＋同一個瀏覽器有效，重整不掉。
- 要進正式檔：把匯出的 JSON 丟給 Claude 說「合併進 v7」，我改寫對應節點的 `note`／衛星規則，合併完補丁歸零。

### 引擎掛點（實際落點）

| 位置 | 內容 |
|---|---|
| 父子去重之後、章節開關之前 | `KWEDIT` 讀檔、`n._kwAuto` 自動值快照、`applyKwEdits()`／`saveKwEdits()`／`setKw()` |
| `draw()` 衛星鏈 | 每禎寫入 `n._kwHit`（世界座標命中框）；`n.kwEdited` 時畫 `✎` |
| `pointerdown`／`pointerup` | 加 `e.button!==0` 擋掉——右鍵不再拖動節點、也不再開詳情面板 |
| script 尾端 | `pickKw()`、三個右鍵選單、清單編輯器、匯出／匯入 |

驗證：`kw_edit_check.mjs`（10 組：套用／改字／改色／增刪／排序／空字串過濾／不掛／還原／存檔重載／命中測試），
與既有 6 組合檢查一起全 PASS。

---

## 1. 操作設計

### 1-1. 右鍵「節點」→ 出現選單

| 選項 | 行為 |
|---|---|
| 🎨 改顏色 | 展開色盤（紅／藍／綠／橘／紫／黃／灰，即分期七色）。點色即改，該節點光暈與標籤同步變色；再選「預設」還原原色 |
| ➕ 新增子節點 | 彈出小表單：標題（必填）、副標、說明。建立後以**實線**接到被點的節點，並依現有規則自動進主題樹排版、自動長衛星點 |
| ✏️ 編輯 | 同上表單，帶入現值改標題／副標／說明。改完衛星點自動重算（配對句型、演變箭頭等規則照常生效） |
| 🗑 刪除 | 隱藏該節點與其全部連線（**軟刪除**，可還原）。若它有「專屬子孫」（樹上掛在它下面的），一併詢問是否連同隱藏 |
| ↩ 還原此節點 | 清掉此節點的顏色／編輯／刪除變更，回到檔案原始值 |

### 1-2. 右鍵「空白處」→ 出現選單

| 選項 | 行為 |
|---|---|
| ➕ 新增獨立節點 | 表單多兩欄：章別（①～⑤）、層（事件／人物／地點／重點／主題）。建立後落在對應層，之後可再用「新增子節點」接線 |
| 💾 匯出變更 | 下載 `關係圖變更_YYYYMMDD.json`（見 §3 格式）。要進正式檔就把這個檔丟給 Claude 合併，或自行照格式貼回資料區 |
| ↩ 還原全部 | 清空所有變更（有確認提示），回到檔案原始狀態 |

### 1-3. 互動細節

- 選單樣式沿用現有 Apple 風格（`--card` 底、`--hairline` 邊、圓角 12px），深淺色主題自動適配。
- 點選單外任意處或按 `Esc` 關閉；右鍵選單出現時**抑制瀏覽器原生選單**（僅在畫布上，側欄與表格不攔截）。
- 表單用同一個覆蓋層（現有 `#ovl`）呈現，Enter 送出、Esc 取消。
- 刪除的節點在「搜尋」與「▦ 比較表掛載」中同步消失；還原後回來。

---

## 2. 資料模型：覆蓋層（Patch Layer）

**原則：完全不改動檔案內的原始資料（`N`／`E`），所有變更存成一層「補丁」，載入時套用。**
好處：可整體還原、可匯出、原始資料永遠是乾淨的權威版本。

```js
// 全域補丁物件（載入時從 localStorage 讀回）
EDITS = {
  ver: 1,                        // 補丁格式版本
  base: "v6",                    // 產生時的檔案版本（對不上時提出警告，仍嘗試套用）
  colors: { "eYs": "green" },    // 節點id → 分期色名（red/blue/green/orange/purple/yellow/gray）
  hidden: [ "eL2" ],             // 軟刪除的節點 id
  edits:  { "eBn": { label:"…", sub:"…", note:"…" } },   // 覆寫欄位（只存有改的欄）
  addedNodes: [                  // 新增節點：id 一律 "u1","u2"…（u=user，避免與原始 id 撞名）
    { id:"u1", ch:3, kind:"ex", era:"qpost", label:"新節點", sub:"", note:"" }
  ],
  addedEdges: [ ["e3Liu","u1","補充",0] ]   // 與資料區 E 同格式
}
```

### 套用順序（載入時）

```
讀 N/E（原始）→ 套 EDITS（增節點→增連線→覆寫欄位→標記 hidden）
→ 既有管線照跑：衛星點計算 → 佈局 layout() → 繪製
```

- `hidden` 節點：`chVis()` 加一條 `!EDITS.hidden.includes(n.id)`，其餘引擎不用改（連線因端點不可見自動不畫；樹的認領自動跳過）。
- `colors`：`nodeColor()` 開頭先查 `EDITS.colors[n.id]`，命中直接回傳對應 CSS 變數色。
- `edits`／`addedNodes`：在衛星點計算**之前**套用，所以配對句型（在X建Y城、X市的A、X人＝A、A→B）在新增／編輯的說明文裡**照常生效**——這是把編輯放在覆蓋層而不是畫完再貼皮的主因。

---

## 3. 儲存與匯出

### 3-1. localStorage（自動）

- Key：`twhist_edits_v6`（跟檔名版本綁定）。
- 每次變更即存；重新整理、關檔重開都保留。
- **限制（要知道的）**：
  1. 只在**同一台電腦＋同一個瀏覽器**有效；換電腦、換瀏覽器、清瀏覽資料就沒了。
  2. 檔案路徑改變（例如改檔名重放）在部分瀏覽器會被視為不同來源，補丁可能讀不到。
  3. 所以「重要的變更」請定期**匯出 JSON**。

### 3-2. 匯出 JSON → 合併回正式檔

- 匯出的就是 §2 的 `EDITS` 物件（美化排版）。
- 合併方式二選一：
  - **丟給 Claude**：「把這個變更檔合併進 v6」→ 我把 addedNodes/addedEdges 寫進資料區、edits 直接改原節點、hidden 刪除該列、colors 換 era 或加色欄，合併完補丁歸零。
  - **自行貼回**：addedNodes 每筆貼進 `N`、addedEdges 貼進 `E`（格式一模一樣），其餘手動改。

---

## 4. 引擎掛點（實作位置清單）

| # | 掛點 | 修改內容 |
|---|---|---|
| 1 | 資料載入後、衛星點計算前 | 新增「套用 EDITS」區塊（增節點入 `nodes`／`byId`、增連線入 `links`／`nbrs`／`adj`、覆寫欄位） |
| 2 | `chVis()` | 加 hidden 判斷 |
| 3 | `nodeColor()` | 先查 `EDITS.colors` |
| 4 | `canvas contextmenu` 事件 | `preventDefault` → `pick()` 命中節點→節點選單；否則→空白選單 |
| 5 | 新增 `#ctxMenu` DOM＋CSS | 選單本體與色盤 |
| 6 | 新增表單（借用 `#ovl`） | 標題／副標／說明（＋章別／層，僅新增獨立節點時） |
| 7 | 變更後統一呼叫 | `rebuildKw(node)`（單節點衛星重算）→ `layout()` → `autoFit=true` → `saveEdits()` |
| 8 | 搜尋 `doSearch()` | 過濾 hidden（一行） |
| 9 | 匯出／還原 | JSON 下載（`Blob`＋`a[download]`）、`localStorage.removeItem` |

估計：**約 250～300 行**新程式，集中在引擎區尾端；原始資料區零改動。

---

## 5. 邊界情況與規則互動

| 情況 | 處理 |
|---|---|
| 刪除的節點是別人的樹狀父節點 | 詢問「連同 N 個專屬子節點一起隱藏？」；只隱藏自己時，子節點會回到一般列（因樹的認領規則自動跳過 hidden 父） |
| 刪除後該節點在比較表掛載（TBL_MAP） | 表本身不受影響（仍在 ▦ 選單），只是少了一個節點入口 |
| 新增節點的衛星點 | 說明文照現有全部規則跑（重點字比對、四種配對句型、A→B 合併、停用詞、去重） |
| 新增節點與★重點字點亮 | 標題含 3 字以上紅藍重點字時同樣會被點亮 |
| 編輯後與父子去重的互動 | 重算時整體重跑一次去重，維持「一條鏈不見同字兩次」 |
| id 撞名 | 使用者新增一律 `u+流水號`，與原始 id 空間隔離 |
| 檔案日後更新成 v7 | 補丁以節點 id 為鍵，id 沒變就能沿用；base 欄位不符時載入提示「補丁來自 v6，套用可能不完整」 |

---

## 6. 明確不做（本階段）

- 不做拖曳改連線、不做連線的右鍵編輯（先做節點）。
- 不做多人協作／雲端同步。
- 不做 undo 歷史堆疊（提供「還原此節點」與「還原全部」兩級即可）。
- 不改比較表內容的右鍵編輯（表格仍以資料區為準）。

---

## 7. 實作順序（確認後）

1. Patch Layer＋localStorage 讀寫（含 hidden／colors 兩個最簡單的通道）→ 先驗證還原與重載
2. 右鍵選單 UI＋改顏色＋刪除＋還原
3. 編輯表單（編輯現有節點）
4. 新增子節點／獨立節點
5. 匯出 JSON
6. 全檔體檢（沿用既有 12 組合檢查腳本）

每步完成都可先交付試用，不必等全部做完。
