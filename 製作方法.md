# 歷史複習工具（記憶關係圖） 製作方法

> 線上版：<https://anitatsai13.github.io/taiwan-history-graph/>
> 與其他學習 App 不同：這是**多頁面**專案（一個入口＋多張獨立單檔工具頁），每頁仍是自包含 HTML、無框架、無 build step。

## 檔案結構與角色

| 檔案 | 是什麼 |
|---|---|
| `index.html` | 入口頁（Pages 首頁）：臺灣史＋中國與東亞史兩區的導覽 |
| `graph.html` | **臺灣史記憶關係圖**（主檔，約 364KB）：第一・二冊五章，315 節點／523 邊／53 張比較表 |
| `cn-graph.html` | **中國與東亞史記憶關係圖**（約 215KB）：第三・四冊單元 5–10 |
| `admin-map.html` | 行政區劃演變地圖：康熙～戰後八期彩色分區切換，附調整原因 |
| `mindmap.html` | 橫式心智圖（資料較舊，待重建） |
| `data.md` | **文字版資料源**（約 210KB）：按「跨冊總表 → 第一冊 → 第二冊 → 跨冊共用」編排 |
| `sync.ps1` | 同步腳本：把 Downloads 的工作檔複製進 repo（工作檔在外部環境編修） |

## 關係圖的做法（graph.html / cn-graph.html）

- **`<canvas id="graph">` 自繪**，不用 vis-network / d3 等圖形庫——節點、邊、佈局、縮放、點擊全部手寫，保持零依賴。
- 資料＝內嵌 JS 常數：節點（事件／人物／制度）＋邊（因果、先後、對比關聯）＋比較表。
- **冊籤／章籤**互動：標頭冊籤按一下只看該冊、再按展開全部；章籤可單章開關——大圖靠分層收合維持可讀。
- 節點點擊顯示詳情；比較表以卡片呈現在圖旁。
- 臺灣史與中國史**各一張圖**（不同科目、不同年代尺度，硬合成一張會互相干擾）。

## 資料維護模式（與其他 App 最大差異）

- 內容編修在**外部環境**（Downloads 的工作檔），完成後在 repo 目錄執行 `.\sync.ps1` 把對應檔案複製進來再 commit。
- `data.md` 與圖同步擴充（目前 3714 行），是「文字版」複習資料，也是圖資料的對照源。
- 版本戳記在圖標頭（如「單元 5–10 全」），改版要更新。

## 維護流程

1. 外部編修工作檔 → `.\sync.ps1` 同步進 repo
2. 瀏覽器開 `graph.html` 實測（收合、點擊、縮放）
3. commit → push → GitHub Pages 自動部署
